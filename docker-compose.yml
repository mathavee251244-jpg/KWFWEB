version: "3.9"

services:
  kwfweb:
    build: .
    container_name: kingwellfirst-web
    ports:
      - "8080:80"
    restart: unless-stopped
    volumes:
      - ./assets:/usr/share/nginx/html/assets:ro
